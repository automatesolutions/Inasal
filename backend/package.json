{
  "name": "backend",
  "version": "0.1.0",
  "description": "Backend API for Bacolod Tourist AI",
  "private": true,
  "scripts": {
    "dev": "poetry run uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "build": "echo 'Backend build step - install dependencies via poetry'",
    "test": "poetry run pytest",
    "lint": "poetry run ruff check . && poetry run black --check . && poetry run mypy .",
    "format": "poetry run black . && poetry run ruff format .",
    "ingest": "poetry run python scripts/ingest_attractions.py",
    "evaluate": "poetry run python scripts/evaluate_llm_responses.py",
    "test-api": "poetry run python scripts/test_api_endpoints.py"
  },
  "keywords": [],
  "author": "",
  "license": "MIT"
}

